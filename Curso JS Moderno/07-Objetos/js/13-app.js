const producto = {
    nombre: "Monitor de 20 pulgadas",
    precio: 200,
    stock: true,
}

console.log(Object.keys(producto)); // Retorna las llaves

console.log(Object.values(producto));   // Retorna los valores de las llaves

console.log(Object.entries(producto)); // Retorna todo

console.log(producto);