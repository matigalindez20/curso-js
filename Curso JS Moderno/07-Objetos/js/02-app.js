const producto = {
    nombre: "Monitor de 20 pulgadas",
    precio: 200,
    stock: true
}

// Dos formas de acceder a los valores de un objecto

console.log(producto.nombre);
console.log(producto["nombre"]);