const producto = 'monitor de 20" ';
const precio = "20USD";

// Concatenar dos cadenas de texto

console.log(producto.concat(precio));
console.log(producto.concat("en desucento"));

console.log("El producto es un " + producto + "y el precio es " + precio);

console.log(`El producto es ${producto}y tiene un precio de ${precio}`)