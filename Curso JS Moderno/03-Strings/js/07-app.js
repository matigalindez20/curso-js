const producto = "Monitor de 20 pulgadas"
console.log(producto.toUpperCase()); // Cambia todo el texto en Mayúsculas.

console.log(producto.toLowerCase()); // Cambia todo el texto en Minúsculas.

const precio = 300;
console.log(precio);
console.log(precio.toString());